interface:
  display_name: "OR/MS Writing"
  short_description: "Natural OR/MS prose, math exposition, and manuscript judgment"
  default_prompt: "Use $or-ms-writing to draft, rewrite, or audit this OR/MS material for natural English, sound argument order, reviewer calibration, mathematical exposition, citation fit, and body/appendix placement."
policy:
  allow_implicit_invocation: true
